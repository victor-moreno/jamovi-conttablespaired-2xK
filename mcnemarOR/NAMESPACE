# Hand-maintained, not generated by roxygen2: this package has a single
# analysis file, so roxygenise() would delete the import()/importFrom() lines
# below (nothing in R/ carries an @import tag for them) rather than reproduce
# them. Do not run devtools::document()/roxygenise() on this package.

export(contTablesPairedOR)
import(jmvcore)
importFrom(jmvcore,.)
